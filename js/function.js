const marks = [2, 3, 4, 5, 6];
marks.forEach(function(n) {
    console.log(n);
})

// Arrow function
// marks.forEach((n, index) => console.log(n));

// map 
// const square = marks.map((n) => n * n)
// console.log(n);